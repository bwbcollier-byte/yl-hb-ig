[project]
name = "yl-hb-ig"
version = "0.1.0"
description = "Instagram enrichment via RapidAPI"
requires-python = ">=3.12"
dependencies = [
  "requests",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
